server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/manhuntdb
    username: YOUR_USERNAME
    password: YOUR_PASSWORD
  jpa:
    hibernate:
      ddl-auto: #update
    properties:
      hibernate:
        show_sql: #true
        format_sql: #true